require 'spec_helper'

describe SlickdealsCli do
  it 'has a version number' do
    expect(SlickdealsCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
